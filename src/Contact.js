import React, { Component } from 'react'

export default props => (
            <div className="contactmain">
                <div className="contactpic"><img className="smallpic" src={props.thumbnail}/> </div> <div className="contactname">{props.name}</div> 
                
                
            </div>
        )
 

